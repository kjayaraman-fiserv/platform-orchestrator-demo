
version: '3'
services:
  app:
    build: ./app
    ports:
      - "3000:3000"
  web:
    image: nginx:latest
    volumes:
      - ./web/nginx.conf:/etc/nginx/nginx.conf
    ports:
      - "80:80"
    depends_on:
      - app
